name: Build
on:
  - push

jobs:
  build:
    uses: cloud-cli/workflows/.github/workflows/docker-ghcr-build.yml@main
    with:
      name: cloud-cli/vite
      withTag: true
      arch: linux/amd64,linux/arm64
